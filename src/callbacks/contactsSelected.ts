import { IWidgetExtended } from './../interfaces/widget-extended.interface';

const contactsSelected = function (this: IWidgetExtended) {
  console.log('contacts selected');
};

export { contactsSelected };
